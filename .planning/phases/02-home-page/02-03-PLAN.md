---
phase: 02-home-page
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/sections/Projects.tsx
  - components/sections/Resume.tsx
  - public/resume.pdf
autonomous: true
requirements: [PROJ-01, PROJ-03, PROJ-04, RESUME-01]

must_haves:
  truths:
    - "Visitor can browse a card grid showing project titles, descriptions, tech stack tags, and links"
    - "Each project card has tech stack pill tags"
    - "Featured projects have a Featured badge on their card"
    - "Cards with live or GitHub links show clickable icon links"
    - "Cards lift on hover with deepened shadow"
    - "Visitor can download a resume PDF via a clearly labeled button"
  artifacts:
    - path: "components/sections/Projects.tsx"
      provides: "Project card grid consuming lib/projects.ts"
      contains: "allProjects"
      min_lines: 30
    - path: "components/sections/Resume.tsx"
      provides: "Resume download section"
      contains: "resume.pdf"
    - path: "public/resume.pdf"
      provides: "Placeholder PDF so download link works"
  key_links:
    - from: "components/sections/Projects.tsx"
      to: "lib/projects.ts"
      via: "import allProjects"
      pattern: "import.*allProjects.*from.*@/lib/projects"
    - from: "components/sections/Resume.tsx"
      to: "public/resume.pdf"
      via: "anchor href /resume.pdf"
      pattern: 'href="/resume.pdf"'
---

<objective>
Build the Projects card grid and Resume download section. Projects consumes the data layer from lib/projects.ts. Resume provides a download button with a placeholder PDF.

Purpose: Projects is the portfolio's showcase — where a visitor evaluates DJ's work. Resume provides a traditional takeaway for recruiters.

Output: Fully styled Projects.tsx with card grid, Resume.tsx with download button, and placeholder resume.pdf in public/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-RESEARCH.md
@.planning/phases/02-home-page/02-01-SUMMARY.md

<interfaces>
<!-- Data layer consumed by Projects section -->

From lib/projects.ts:
```ts
export interface Project {
  id: string
  slug: string
  title: string
  description: string
  techStack: string[]
  links: { live?: string; github?: string }
  featured: boolean
  problem: string
  role: string
  keyDecisions: string
  outcome: string
}

export const projects: Project[] = [...]
export const featuredProjects = projects.filter((p) => p.featured)
export const allProjects = projects
```

From components/sections/Projects.tsx (current stub — replace entirely):
```tsx
export function Projects() {
  return (
    <section id="projects" className="py-24 px-8">
      <h2 className="font-display text-3xl font-bold">Projects</h2>
      <p className="mt-4 text-gray-600">Featured work coming soon.</p>
    </section>
  )
}
```

From components/sections/Resume.tsx (current stub — replace entirely):
```tsx
export function Resume() {
  return (
    <section id="resume" className="py-24 px-8">
      <h2 className="font-display text-3xl font-bold">Resume</h2>
      <p className="mt-4 text-gray-600">Download link coming soon.</p>
    </section>
  )
}
```

From app/globals.css (after Plan 01):
- bg-card = pure white #FFFFFF (subtle lift over cream bg)
- border-border = warm gray
- bg-muted = warm light gray (tech tag background)
- text-muted-foreground = warm mid-tone (descriptions, tags)
- bg-primary = terracotta (download button)
- text-primary = terracotta (hover state for icon links)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Projects section with card grid</name>
  <files>components/sections/Projects.tsx</files>
  <read_first>
    - components/sections/Projects.tsx (current stub — will be fully replaced)
    - lib/projects.ts (data source — Project interface, allProjects export, check field names: title, description, techStack, links.live, links.github, featured)
    - .planning/phases/02-home-page/02-UI-SPEC.md (Projects Section + Project Card component inventory — exact Tailwind classes for grid, cards, tags, links, badges)
    - .planning/phases/02-home-page/02-RESEARCH.md (Pattern 7: Project Card with Hover Lift)
  </read_first>
  <action>
Replace the entire contents of `components/sections/Projects.tsx`. Server Component (no `'use client'`). Named export `Projects`.

Import `allProjects` from `@/lib/projects`, `ExternalLink` and `Github` from `lucide-react`, and the `Project` type.

Structure:
```tsx
import { allProjects, type Project } from '@/lib/projects'
import { ExternalLink, Github } from 'lucide-react'

function ProjectCard({ project }: { project: Project }) {
  return (
    <article className="group rounded-xl border border-border bg-card p-6 shadow-sm transition-all duration-200 hover:-translate-y-1 hover:shadow-md">
      {/* Header: title + featured badge */}
      <div className="flex items-start justify-between gap-2">
        <h3 className="font-display text-sm font-bold text-foreground">
          {project.title}
        </h3>
        {project.featured && (
          <span className="shrink-0 rounded-full bg-primary/10 px-2 py-0.5 text-sm text-primary">
            Featured
          </span>
        )}
      </div>

      {/* Description */}
      <p className="mt-2 text-sm text-muted-foreground">
        {project.description}
      </p>

      {/* Tech stack tags */}
      <div className="mt-4 flex flex-wrap gap-2">
        {project.techStack.map((tech) => (
          <span
            key={tech}
            className="rounded-full bg-muted px-3 py-1 text-sm text-muted-foreground"
          >
            {tech}
          </span>
        ))}
      </div>

      {/* Links */}
      {(project.links.live || project.links.github) && (
        <div className="mt-4 flex gap-3 border-t border-border pt-4">
          {project.links.live && (
            <a
              href={project.links.live}
              target="_blank"
              rel="noopener noreferrer"
              aria-label={`View ${project.title} live site`}
              className="text-muted-foreground transition-colors hover:text-primary"
            >
              <ExternalLink className="h-4 w-4" />
            </a>
          )}
          {project.links.github && (
            <a
              href={project.links.github}
              target="_blank"
              rel="noopener noreferrer"
              aria-label={`View ${project.title} on GitHub`}
              className="text-muted-foreground transition-colors hover:text-primary"
            >
              <Github className="h-4 w-4" />
            </a>
          )}
        </div>
      )}
    </article>
  )
}

export function Projects() {
  if (allProjects.length === 0) {
    return (
      <section id="projects" className="px-6 py-24">
        <div className="mx-auto max-w-5xl">
          <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
            Work
          </h2>
          <p className="mt-4 text-muted-foreground">No projects yet.</p>
          <p className="text-sm text-muted-foreground">Check back soon.</p>
        </div>
      </section>
    )
  }

  return (
    <section id="projects" className="px-6 py-24">
      <div className="mx-auto max-w-5xl">
        <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
          Work
        </h2>
        <div className="mt-12 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {allProjects.map((project) => (
            <ProjectCard key={project.id} project={project} />
          ))}
        </div>
      </div>
    </section>
  )
}
```

Key requirements per UI-SPEC:
- Section: `id="projects"`, `px-6 py-24`
- Container: `mx-auto max-w-5xl`
- Heading: "Work" (not "Projects") — `font-display text-3xl md:text-4xl font-bold text-foreground`
- Grid: `mt-12 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
- Card: `<article>` element, `group rounded-xl border border-border bg-card p-6 shadow-sm`
- Card hover: `transition-all duration-200 hover:-translate-y-1 hover:shadow-md`
- Card title: `font-display text-sm font-bold text-foreground`
- Card description: `text-sm text-muted-foreground mt-2`
- Featured badge: `rounded-full bg-primary/10 text-primary px-2 py-0.5 text-sm` — only shown when `project.featured === true`
- Tech tags: `flex flex-wrap gap-2 mt-4`, each tag `rounded-full bg-muted text-muted-foreground px-3 py-1 text-sm`
- Links row: `flex gap-3 mt-4 pt-4 border-t border-border` — only rendered if live or github link exists
- Live link: `ExternalLink` icon `h-4 w-4`, `text-muted-foreground hover:text-primary transition-colors`, `aria-label="View [title] live site"`
- GitHub link: `Github` icon `h-4 w-4`, same styles, `aria-label="View [title] on GitHub"`
- Empty state: "No projects yet." and "Check back soon." (per UI-SPEC Copywriting Contract)
- Data source: `allProjects` from `@/lib/projects` — NEVER hardcode project data
- NO `'use client'` — Server Component
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && grep -q "allProjects" components/sections/Projects.tsx && grep -q "from '@/lib/projects'" components/sections/Projects.tsx && grep -q "hover:-translate-y-1" components/sections/Projects.tsx && grep -q "hover:shadow-md" components/sections/Projects.tsx && grep -q "Featured" components/sections/Projects.tsx && grep -q "bg-primary/10" components/sections/Projects.tsx && grep -q "rounded-full bg-muted" components/sections/Projects.tsx && grep -q "ExternalLink" components/sections/Projects.tsx && grep -q "Github" components/sections/Projects.tsx && grep -q "No projects yet" components/sections/Projects.tsx && grep -q "sm:grid-cols-2" components/sections/Projects.tsx && grep -q "lg:grid-cols-3" components/sections/Projects.tsx && ! grep -q "use client" components/sections/Projects.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Projects.tsx contains `id="projects"`
    - Projects.tsx contains `px-6 py-24` (section padding)
    - Projects.tsx contains `max-w-5xl` (container)
    - Projects.tsx contains `allProjects` imported from `@/lib/projects`
    - Projects.tsx contains `type Project` import
    - Projects.tsx contains heading text "Work"
    - Projects.tsx contains `font-display text-3xl` and `md:text-4xl` and `font-bold` (heading)
    - Projects.tsx contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (responsive grid)
    - Projects.tsx contains `gap-6 mt-12` (grid spacing)
    - Projects.tsx contains `<article` (card element)
    - Projects.tsx contains `group rounded-xl border border-border bg-card p-6 shadow-sm` (card base)
    - Projects.tsx contains `transition-all duration-200 hover:-translate-y-1 hover:shadow-md` (card hover)
    - Projects.tsx contains `font-display text-sm font-bold` (card title)
    - Projects.tsx contains `text-sm text-muted-foreground` (card description)
    - Projects.tsx contains `bg-primary/10` and `text-primary` (featured badge)
    - Projects.tsx contains `rounded-full bg-muted` and `px-3 py-1` (tech tags)
    - Projects.tsx contains `ExternalLink` and `Github` imports from lucide-react
    - Projects.tsx contains `aria-label` on link elements
    - Projects.tsx contains `hover:text-primary transition-colors` (icon link hover)
    - Projects.tsx contains `border-t border-border` (links row separator)
    - Projects.tsx contains "No projects yet." (empty state)
    - Projects.tsx does NOT contain `use client`
    - Projects.tsx does NOT hardcode project data inline
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Projects section renders a responsive card grid (1/2/3 columns) consuming allProjects from lib/projects.ts. Each card shows title, description, tech stack tags, featured badge (when applicable), and live/GitHub icon links. Cards lift on hover.</done>
</task>

<task type="auto">
  <name>Task 2: Build Resume section and create placeholder PDF</name>
  <files>components/sections/Resume.tsx, public/resume.pdf</files>
  <read_first>
    - components/sections/Resume.tsx (current stub — will be fully replaced)
    - .planning/phases/02-home-page/02-UI-SPEC.md (Resume Section component inventory — exact Tailwind classes, copy, download link)
    - .planning/phases/02-home-page/02-CONTEXT.md (D-21, D-22 — resume layout and placeholder PDF requirement)
  </read_first>
  <action>
**Step 1: Create placeholder PDF**

Create a minimal placeholder PDF at `public/resume.pdf`. Use a simple approach: create a valid minimal PDF file. The simplest way is to use Node.js to write a minimal valid PDF:

```bash
node -e "
const pdf = '%PDF-1.4\n1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\nendobj\n3 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Contents 4 0 R /Resources << >> >>\nendobj\n4 0 obj\n<< /Length 44 >>\nstream\nBT /F1 12 Tf 100 700 Td (Placeholder) Tj ET\nendstream\nendobj\nxref\n0 5\n0000000000 65535 f \n0000000009 00000 n \n0000000058 00000 n \n0000000115 00000 n \n0000000236 00000 n \ntrailer\n<< /Size 5 /Root 1 0 R >>\nstartxref\n330\n%%EOF';
require('fs').writeFileSync('public/resume.pdf', pdf);
"
```

This creates a valid PDF that browsers will download. DJ replaces with real resume before launch.

**Step 2: Replace Resume.tsx**

Replace the entire contents of `components/sections/Resume.tsx`. Server Component (no `'use client'`). Named export `Resume`.

Structure:
```tsx
export function Resume() {
  return (
    <section id="resume" className="px-6 py-24">
      <div className="mx-auto max-w-5xl text-center">
        <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
          Resume
        </h2>
        <p className="mt-4 text-base text-muted-foreground">
          Want the full picture? Grab my resume.
        </p>
        <a
          href="/resume.pdf"
          download
          className="mt-8 inline-block rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground transition-colors hover:bg-primary/90"
        >
          Download Resume
        </a>
      </div>
    </section>
  )
}
```

Key requirements per UI-SPEC:
- Section: `id="resume"`, `px-6 py-24`
- Container: `mx-auto max-w-5xl text-center`
- Heading: "Resume" — `font-display text-3xl md:text-4xl font-bold text-foreground`
- Intro: "Want the full picture? Grab my resume." — `mt-4 text-base text-muted-foreground`
- Download button: `<a href="/resume.pdf" download>` — `mt-8 inline-block rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground hover:bg-primary/90`
- Button copy: "Download Resume" (per D-21/D-22)
- The `download` attribute triggers browser download instead of navigation
- NO `'use client'` — Server Component
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && test -f public/resume.pdf && grep -q 'href="/resume.pdf"' components/sections/Resume.tsx && grep -q "download" components/sections/Resume.tsx && grep -q "Download Resume" components/sections/Resume.tsx && grep -q "bg-primary" components/sections/Resume.tsx && grep -q "Want the full picture" components/sections/Resume.tsx && ! grep -q "use client" components/sections/Resume.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - public/resume.pdf exists (placeholder PDF file)
    - Resume.tsx contains `id="resume"`
    - Resume.tsx contains `px-6 py-24` (section padding)
    - Resume.tsx contains `max-w-5xl text-center` (centered layout)
    - Resume.tsx contains heading text "Resume"
    - Resume.tsx contains `font-display text-3xl` and `md:text-4xl` (heading)
    - Resume.tsx contains `Want the full picture? Grab my resume.` (intro copy)
    - Resume.tsx contains `text-muted-foreground` (intro styling)
    - Resume.tsx contains `href="/resume.pdf"` (download link)
    - Resume.tsx contains `download` attribute on the anchor
    - Resume.tsx contains `bg-primary` and `text-primary-foreground` (button)
    - Resume.tsx contains `hover:bg-primary/90` (button hover)
    - Resume.tsx contains `rounded-lg` and `px-8 py-3` (button sizing)
    - Resume.tsx contains `font-bold` (button text weight)
    - Resume.tsx contains "Download Resume" (button copy)
    - Resume.tsx does NOT contain `use client`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Resume section renders centered with heading, intro line, and terracotta download button linking to /resume.pdf. Placeholder PDF exists in public/ so the download works immediately.</done>
</task>

</tasks>

<verification>
1. `npx next build` exits 0
2. Browser: Projects section shows card grid with 3 project cards (2 featured, 1 non-featured)
3. Featured cards have a "Featured" badge pill
4. Cards show tech stack tags as rounded pills
5. Cards lift on hover (translateY -4px, shadow deepens)
6. Resume section shows heading, intro text, and terracotta download button
7. Clicking "Download Resume" downloads the placeholder PDF
8. Grid is responsive: 1 column mobile, 2 columns sm, 3 columns lg
</verification>

<success_criteria>
- Projects grid renders all 3 projects from allProjects (not hardcoded)
- Cards have title (Playfair), description, tech tags (pills), featured badge, icon links
- Card hover lift works (CSS transition, no Motion)
- Resume has centered layout with "Download Resume" terracotta button
- public/resume.pdf exists and is downloadable
- Both are Server Components (no 'use client')
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-03-SUMMARY.md`
</output>
