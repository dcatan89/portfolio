---
phase: 02-home-page
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/sections/Hero.tsx
  - components/sections/About.tsx
autonomous: true
requirements: [HERO-01, HERO-02, ABOUT-01, ABOUT-02]

must_haves:
  truths:
    - "Visitor sees DJ's name in large Playfair Display, a tagline, and a View My Work CTA on first load"
    - "Visitor sees a pulsing green availability badge near the CTA"
    - "Visitor sees a bouncing scroll chevron at the bottom of the hero"
    - "Visitor can read an About section with bio text and a photo placeholder"
    - "About section is two-column on desktop, stacked on mobile"
  artifacts:
    - path: "components/sections/Hero.tsx"
      provides: "Full hero section with name, tagline, CTA, badge, chevron"
      contains: "min-h-screen"
      min_lines: 25
    - path: "components/sections/About.tsx"
      provides: "About section with photo placeholder and bio"
      contains: "grid"
      min_lines: 20
  key_links:
    - from: "components/sections/Hero.tsx"
      to: "#projects"
      via: "CTA anchor link"
      pattern: 'href="#projects"'
    - from: "components/sections/About.tsx"
      to: "lucide-react"
      via: "User icon import for photo placeholder"
      pattern: "User.*lucide-react"
---

<objective>
Build the Hero and About sections — the first two content sections a visitor sees. Hero establishes identity (name, tagline, CTA, availability). About provides depth (bio, photo placeholder).

Purpose: These are the above-the-fold sections. A visitor's first impression comes from Hero; their decision to keep scrolling comes from About.

Output: Fully styled Hero.tsx and About.tsx replacing the Phase 1 stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-RESEARCH.md
@.planning/phases/02-home-page/02-01-SUMMARY.md

<interfaces>
<!-- Existing stubs being replaced -->

From components/sections/Hero.tsx (current stub — replace entirely):
```tsx
export function Hero() {
  return (
    <section id="hero" className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="font-display text-5xl font-bold">DJ Catan</h1>
        <p className="mt-4 text-lg text-gray-600">
          Full-stack developer. Building thoughtful digital products.
        </p>
      </div>
    </section>
  )
}
```

From components/sections/About.tsx (current stub — replace entirely):
```tsx
export function About() {
  return (
    <section id="about" className="py-24 px-8">
      <h2 className="font-display text-3xl font-bold">About</h2>
      <p className="mt-4 max-w-2xl text-gray-600">...</p>
    </section>
  )
}
```

From app/globals.css (after Plan 01 — palette is warm):
- bg-background = warm cream #FAF8F5
- text-foreground = warm charcoal #1C1917
- text-primary = terracotta #C2603A
- text-muted-foreground = warm mid-tone #78716C
- bg-muted = warm light gray
- border-border = warm gray border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero section with CTA, availability badge, and scroll chevron</name>
  <files>components/sections/Hero.tsx</files>
  <read_first>
    - components/sections/Hero.tsx (current stub — will be fully replaced)
    - .planning/phases/02-home-page/02-UI-SPEC.md (Hero Section component inventory — exact Tailwind classes for every element)
    - .planning/phases/02-home-page/02-RESEARCH.md (Pattern 5: Pulsing Availability Badge, Pattern 6: Bouncing Scroll Chevron)
  </read_first>
  <action>
Replace the entire contents of `components/sections/Hero.tsx`. This is a Server Component (no `'use client'`). Named export `Hero`.

Import `ChevronDown` from `lucide-react`.

Structure:
```tsx
import { ChevronDown } from 'lucide-react'

export function Hero() {
  return (
    <section id="hero" className="relative flex min-h-screen flex-col items-center justify-center text-center">
      {/* Name */}
      <h1 className="font-display text-5xl font-bold text-foreground md:text-6xl">
        DJ Catan
      </h1>

      {/* Tagline */}
      <p className="mt-4 text-base text-muted-foreground">
        Full-stack developer building thoughtful digital products.
      </p>

      {/* CTA + Badge group */}
      <div className="mt-8 flex flex-col items-center gap-4">
        {/* CTA Button */}
        <a
          href="#projects"
          className="rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground transition-colors hover:bg-primary/90"
        >
          View My Work
        </a>

        {/* Availability Badge */}
        <div className="flex items-center gap-2 rounded-full border border-border bg-muted px-4 py-1.5">
          <span className="relative flex h-2 w-2">
            <span className="absolute inline-flex h-full w-full animate-ping rounded-full bg-green-400 opacity-75" />
            <span className="relative inline-flex h-2 w-2 rounded-full bg-green-500" />
          </span>
          <span className="text-sm text-muted-foreground">Available for work</span>
        </div>
      </div>

      {/* Scroll chevron */}
      <div className="absolute bottom-8 left-1/2 -translate-x-1/2">
        <ChevronDown className="h-6 w-6 animate-bounce text-muted-foreground" />
      </div>
    </section>
  )
}
```

Key requirements per UI-SPEC:
- Section: `id="hero"`, `relative` (for absolute chevron), `min-h-screen`, `flex flex-col items-center justify-center text-center`
- Name h1: `font-display text-5xl md:text-6xl font-bold text-foreground` (48px mobile, 60px desktop)
- Tagline: `mt-4 text-base text-muted-foreground` (16px Inter)
- CTA: `<a href="#projects">` (plain anchor, NOT Link), `rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground hover:bg-primary/90`
- CTA copy: "View My Work" (per D-11)
- Badge: `mt-4` below CTA, `rounded-full border border-border bg-muted px-4 py-1.5`
- Badge dot: inner `h-2 w-2 rounded-full bg-green-500`, outer `animate-ping bg-green-400 opacity-75`
- Badge text: `text-sm text-muted-foreground`, copy "Available for work" (per D-12)
- Chevron: `absolute bottom-8 left-1/2 -translate-x-1/2`, `ChevronDown` from lucide-react, `h-6 w-6 animate-bounce text-muted-foreground`
- NO Motion library. All animations are Tailwind built-ins (`animate-ping`, `animate-bounce`).
- NO `'use client'` — this is a Server Component.
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && grep -q "min-h-screen" components/sections/Hero.tsx && grep -q "View My Work" components/sections/Hero.tsx && grep -q 'href="#projects"' components/sections/Hero.tsx && grep -q "animate-ping" components/sections/Hero.tsx && grep -q "animate-bounce" components/sections/Hero.tsx && grep -q "Available for work" components/sections/Hero.tsx && grep -q "bg-primary" components/sections/Hero.tsx && grep -q "ChevronDown" components/sections/Hero.tsx && grep -q "bg-green-500" components/sections/Hero.tsx && ! grep -q "use client" components/sections/Hero.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hero.tsx contains `id="hero"`
    - Hero.tsx contains `relative` on the section (for absolute chevron positioning)
    - Hero.tsx contains `min-h-screen`
    - Hero.tsx contains `font-display text-5xl` and `md:text-6xl` and `font-bold` (name heading)
    - Hero.tsx contains `text-muted-foreground` (tagline)
    - Hero.tsx contains `href="#projects"` (CTA link to projects)
    - Hero.tsx contains `bg-primary` and `text-primary-foreground` (CTA button)
    - Hero.tsx contains `View My Work` (CTA copy)
    - Hero.tsx contains `animate-ping` and `bg-green-400` (badge pulse outer)
    - Hero.tsx contains `bg-green-500` (badge dot inner)
    - Hero.tsx contains `Available for work` (badge text)
    - Hero.tsx contains `animate-bounce` (scroll chevron)
    - Hero.tsx contains `ChevronDown` import from lucide-react
    - Hero.tsx contains `absolute bottom-8` (chevron positioning)
    - Hero.tsx does NOT contain `use client`
    - Hero.tsx does NOT contain `motion` import
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Hero section displays DJ's name (Playfair Display, responsive sizing), tagline, terracotta "View My Work" CTA linking to #projects, pulsing green availability badge, and bouncing scroll chevron at bottom.</done>
</task>

<task type="auto">
  <name>Task 2: Build About section with photo placeholder and bio</name>
  <files>components/sections/About.tsx</files>
  <read_first>
    - components/sections/About.tsx (current stub — will be fully replaced)
    - .planning/phases/02-home-page/02-UI-SPEC.md (About Section component inventory — exact Tailwind classes, layout grid, photo placeholder specs)
    - .planning/phases/02-home-page/02-RESEARCH.md (Pattern 8: Photo Placeholder)
  </read_first>
  <action>
Replace the entire contents of `components/sections/About.tsx`. Server Component (no `'use client'`). Named export `About`.

Import `User` from `lucide-react`.

Structure:
```tsx
import { User } from 'lucide-react'

export function About() {
  return (
    <section id="about" className="px-6 py-24">
      <div className="mx-auto grid max-w-5xl grid-cols-1 items-start gap-12 md:grid-cols-2">
        {/* Photo placeholder — left column */}
        <div className="flex justify-center md:justify-start">
          <div className="flex aspect-[3/4] w-full max-w-xs items-center justify-center rounded-2xl bg-[#E8E4DF]">
            <User className="h-16 w-16 text-[#C4B8AE]" />
          </div>
        </div>

        {/* Bio — right column */}
        <div>
          <h2 className="font-display text-3xl font-bold text-foreground md:text-4xl">
            About Me
          </h2>
          <div className="mt-6 space-y-4 leading-relaxed text-foreground">
            <p>
              Hey, I&apos;m DJ. I build things for the web and care about getting
              the details right.
            </p>
            <p>
              With a background spanning full-stack development, I focus on
              creating digital products that are as thoughtful under the hood as
              they are on the surface.
            </p>
            <p>
              When I&apos;m not coding, you can find me exploring new ideas,
              learning something new, or finding ways to make complex things
              simple.
            </p>
          </div>
        </div>
      </div>
    </section>
  )
}
```

Key requirements per UI-SPEC:
- Section: `id="about"`, `px-6 py-24`
- Container: `mx-auto max-w-5xl`
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-12 items-start` — photo left, bio right on desktop; stacked (photo top) on mobile (per D-14, D-16)
- Photo placeholder: `aspect-[3/4] w-full max-w-xs rounded-2xl bg-[#E8E4DF]` with `User` icon `h-16 w-16 text-[#C4B8AE]` centered (per D-15)
- Photo column: centered on mobile (`flex justify-center`), left-aligned on desktop (`md:justify-start`)
- Heading: "About Me" — `font-display text-3xl md:text-4xl font-bold text-foreground` (per UI-SPEC Slot B)
- Bio body: Inter 16px, `leading-relaxed text-foreground`, 3 placeholder paragraphs with `space-y-4`
- All bio text is placeholder — clearly structured for easy swap to real content
- Apostrophes escaped as `&apos;` in JSX
- NO `'use client'` — Server Component
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && grep -q "id=\"about\"" components/sections/About.tsx && grep -q "grid-cols-1 md:grid-cols-2" components/sections/About.tsx && grep -q "aspect-\[3/4\]" components/sections/About.tsx && grep -q "bg-\[#E8E4DF\]" components/sections/About.tsx && grep -q "text-\[#C4B8AE\]" components/sections/About.tsx && grep -q "About Me" components/sections/About.tsx && grep -q "leading-relaxed" components/sections/About.tsx && grep -q "User" components/sections/About.tsx && ! grep -q "use client" components/sections/About.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - About.tsx contains `id="about"`
    - About.tsx contains `px-6 py-24` (section padding)
    - About.tsx contains `max-w-5xl` (container)
    - About.tsx contains `grid-cols-1 md:grid-cols-2` (responsive two-column)
    - About.tsx contains `gap-12` (column gap)
    - About.tsx contains `aspect-[3/4]` (photo aspect ratio)
    - About.tsx contains `max-w-xs` (photo max width)
    - About.tsx contains `rounded-2xl` (photo border radius)
    - About.tsx contains `bg-[#E8E4DF]` (warm gray photo fill)
    - About.tsx contains `text-[#C4B8AE]` (placeholder icon color)
    - About.tsx contains `User` import from lucide-react
    - About.tsx contains `h-16 w-16` (icon size 64px)
    - About.tsx contains `About Me` (heading)
    - About.tsx contains `font-display` and `text-3xl` and `font-bold` (heading styles)
    - About.tsx contains `leading-relaxed` (bio line height)
    - About.tsx does NOT contain `use client`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>About section renders with photo placeholder (3:4 aspect, warm gray, User icon) on left, bio heading + 3 placeholder paragraphs on right. Stacks vertically on mobile.</done>
</task>

</tasks>

<verification>
1. `npx next build` exits 0
2. Browser: Hero fills viewport with name, tagline, CTA button, availability badge, bouncing chevron
3. CTA button is terracotta (#C2603A) and links to #projects
4. Green dot pulses in availability badge
5. About section has two-column layout on desktop (photo left, bio right)
6. Photo placeholder is warm gray with User icon
7. At mobile viewport (< 768px): About stacks vertically (photo top, bio below)
</verification>

<success_criteria>
- Hero: name in Playfair Display (responsive), tagline, terracotta CTA, pulsing badge, bouncing chevron
- About: two-column (md+ breakpoint), photo placeholder with aspect-[3/4], bio with 3 paragraphs
- Both are Server Components (no 'use client')
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-02-SUMMARY.md`
</output>
