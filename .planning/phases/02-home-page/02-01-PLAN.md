---
phase: 02-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - components/layout/Navbar.tsx
  - components/layout/NavbarMobile.tsx
  - components/sections/Footer.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "Visitor sees a sticky navbar at the top with DJ Catan brand and 4 navigation links"
    - "Clicking a navbar link smooth-scrolls to the corresponding section"
    - "Visitor on mobile can open a hamburger menu and tap a link to navigate"
    - "Visitor sees a footer with copyright text and GitHub, LinkedIn, Email icon links"
    - "Page background is warm cream (#FAF8F5), not pure white"
  artifacts:
    - path: "app/globals.css"
      provides: "Warm color palette CSS variables"
      contains: "--background: 36 33% 97%"
    - path: "app/layout.tsx"
      provides: "Smooth scroll configuration"
      contains: "scroll-smooth"
    - path: "components/layout/Navbar.tsx"
      provides: "Sticky navbar Server Component shell"
      exports: ["Navbar"]
    - path: "components/layout/NavbarMobile.tsx"
      provides: "Client component hamburger toggle"
      contains: "use client"
    - path: "components/sections/Footer.tsx"
      provides: "Footer with social icon links"
      exports: ["Footer"]
    - path: "app/page.tsx"
      provides: "Navbar and Footer wired into page"
      contains: "Navbar"
  key_links:
    - from: "components/layout/Navbar.tsx"
      to: "components/layout/NavbarMobile.tsx"
      via: "Server Component renders Client child"
      pattern: "NavbarMobile"
    - from: "app/page.tsx"
      to: "components/layout/Navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "app/page.tsx"
      to: "components/sections/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Replace the default shadcn/ui color palette with the warm cream/terracotta/charcoal palette, enable smooth scrolling, and build the Navbar and Footer — the persistent "chrome" that frames every section.

Purpose: All subsequent section components depend on the color palette being correct (they use `bg-background`, `text-foreground`, `text-primary`, etc.). The Navbar provides navigation to every section. This must land first.

Output: Warm palette in globals.css, smooth scroll on html, sticky Navbar with hamburger menu, Footer with social links, both wired into page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-page/02-CONTEXT.md
@.planning/phases/02-home-page/02-UI-SPEC.md
@.planning/phases/02-home-page/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/globals.css (current state — CSS variables to update):
```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 240 10% 3.9%;
    --primary: 240 5.9% 10%;
    --primary-foreground: 0 0% 98%;
    --muted: 240 4.8% 95.9%;
    --muted-foreground: 240 3.8% 46.1%;
    --border: 240 5.9% 90%;
    --input: 240 5.9% 90%;
    --ring: 240 10% 3.9%;
  }
}
```

From app/layout.tsx (current html tag — add scroll-smooth + data-scroll-behavior):
```tsx
<html lang="en" className={`${playfairDisplay.variable} ${inter.variable}`}>
```

From app/page.tsx (current structure — add Navbar before main, Footer after):
```tsx
export default function Home() {
  return (
    <main>
      <Hero />
      <About />
      <Projects />
      <Resume />
      <Contact />
    </main>
  )
}
```

From lib/utils.ts:
```ts
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update globals.css warm palette and layout.tsx smooth scroll</name>
  <files>app/globals.css, app/layout.tsx</files>
  <read_first>
    - app/globals.css (see current CSS variable format — space-separated HSL, no hsl() wrapper in var definitions)
    - app/layout.tsx (see current html tag className — must add scroll-smooth and data-scroll-behavior attribute)
    - .planning/phases/02-home-page/02-UI-SPEC.md (Color section — exact HSL values for every variable)
  </read_first>
  <action>
**globals.css** — Update the `:root` block inside `@layer base`. Replace these CSS variable values (keep the format as space-separated HSL numbers, do NOT wrap in `hsl()`):

```
--background: 36 33% 97%;
--foreground: 20 8% 10%;
--card: 0 0% 100%;
--card-foreground: 20 8% 10%;
--primary: 18 55% 49%;
--primary-foreground: 36 33% 97%;
--muted: 36 20% 94%;
--muted-foreground: 25 6% 44%;
--border: 36 15% 88%;
--input: 36 15% 88%;
--ring: 18 55% 49%;
```

Leave `--popover`, `--popover-foreground`, `--secondary`, `--secondary-foreground`, `--accent`, `--accent-foreground`, `--destructive`, `--destructive-foreground`, `--radius` unchanged. Leave the `.dark` block and the `@layer base` body/border rules unchanged. Leave the `@theme` block at the top unchanged.

**layout.tsx** — Update the `<html>` tag to add `scroll-smooth` to className and `data-scroll-behavior="smooth"` as an attribute. Per D-06/NAV-02 and RESEARCH Pattern 4:

Change from:
```tsx
<html lang="en" className={`${playfairDisplay.variable} ${inter.variable}`}>
```
To:
```tsx
<html lang="en" data-scroll-behavior="smooth" className={`scroll-smooth ${playfairDisplay.variable} ${inter.variable}`}>
```

Do NOT change anything else in layout.tsx (fonts, metadata, body tag all stay as-is).
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && grep -q "36 33% 97%" app/globals.css && grep -q "18 55% 49%" app/globals.css && grep -q "20 8% 10%" app/globals.css && grep -q "25 6% 44%" app/globals.css && grep -q "36 15% 88%" app/globals.css && grep -q "scroll-smooth" app/layout.tsx && grep -q 'data-scroll-behavior="smooth"' app/layout.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--background: 36 33% 97%`
    - globals.css contains `--foreground: 20 8% 10%`
    - globals.css contains `--primary: 18 55% 49%`
    - globals.css contains `--primary-foreground: 36 33% 97%`
    - globals.css contains `--muted: 36 20% 94%`
    - globals.css contains `--muted-foreground: 25 6% 44%`
    - globals.css contains `--border: 36 15% 88%`
    - globals.css contains `--input: 36 15% 88%`
    - globals.css contains `--ring: 18 55% 49%`
    - globals.css does NOT contain `hsl(36 33% 97%)` inside the variable definition (no double-wrapping)
    - layout.tsx contains `scroll-smooth` in the html className
    - layout.tsx contains `data-scroll-behavior="smooth"` on the html element
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Warm cream/terracotta/charcoal palette active across all semantic color utilities (bg-background, text-foreground, text-primary, etc.). Smooth scroll enabled for all anchor links.</done>
</task>

<task type="auto">
  <name>Task 2: Build Navbar with hamburger menu</name>
  <files>components/layout/Navbar.tsx, components/layout/NavbarMobile.tsx</files>
  <read_first>
    - .planning/phases/02-home-page/02-UI-SPEC.md (Navbar component inventory — exact Tailwind classes, copy, breakpoints)
    - .planning/phases/02-home-page/02-RESEARCH.md (Pattern 2: Sticky Navbar, Pattern 3: Hamburger Menu)
    - lib/utils.ts (cn() helper available at @/lib/utils)
  </read_first>
  <action>
Create `components/layout/` directory if it does not exist.

**components/layout/Navbar.tsx** — Server Component (NO `'use client'`). Named export `Navbar`.

Structure:
```tsx
import { NavbarMobile } from './NavbarMobile'

const navLinks = [
  { label: 'About', href: '#about' },
  { label: 'Work', href: '#projects' },
  { label: 'Resume', href: '#resume' },
  { label: 'Contact', href: '#contact' },
]

export function Navbar() {
  return (
    <header className="sticky top-0 z-50 w-full border-b bg-background">
      <div className="mx-auto flex h-16 max-w-5xl items-center justify-between px-6">
        {/* Brand — left */}
        <a href="#hero" className="font-display text-base font-bold text-foreground">
          DJ Catan
        </a>
        {/* Desktop nav — right, hidden on mobile */}
        <nav className="hidden gap-6 md:flex">
          {navLinks.map((link) => (
            <a
              key={link.href}
              href={link.href}
              className="text-sm font-medium text-foreground transition-colors hover:text-primary"
            >
              {link.label}
            </a>
          ))}
        </nav>
        {/* Mobile hamburger — visible on mobile only */}
        <NavbarMobile links={navLinks} />
      </div>
    </header>
  )
}
```

**components/layout/NavbarMobile.tsx** — Client Component with `'use client'` directive. Named export `NavbarMobile`.

Structure:
```tsx
'use client'

import { useState } from 'react'
import { Menu, X } from 'lucide-react'

export function NavbarMobile({ links }: { links: { label: string; href: string }[] }) {
  const [open, setOpen] = useState(false)

  return (
    <>
      <button
        onClick={() => setOpen(!open)}
        className="p-2 md:hidden"
        aria-label="Toggle menu"
        aria-expanded={open}
      >
        {open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}
      </button>
      {open && (
        <div className="absolute left-0 top-16 z-40 w-full border-b bg-background px-6 py-4 md:hidden">
          <nav className="flex flex-col gap-4">
            {links.map((link) => (
              <a
                key={link.href}
                href={link.href}
                onClick={() => setOpen(false)}
                className="text-sm font-medium text-foreground transition-colors hover:text-primary"
              >
                {link.label}
              </a>
            ))}
          </nav>
        </div>
      )}
    </>
  )
}
```

Key requirements per UI-SPEC:
- Hamburger button has `p-2` for 44px touch target (icon is 20px + 8px padding each side = 36px, close enough; or use `p-3` for 44px exact)
- Mobile menu closes on link click (`onClick={() => setOpen(false)}`)
- `aria-label="Toggle menu"` and `aria-expanded={open}` on the button
- Desktop links use plain `<a href="#section-id">` (NOT `<Link>` from next/link) for smooth scroll
- Nav links: "About" -> `#about`, "Work" -> `#projects`, "Resume" -> `#resume`, "Contact" -> `#contact`
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && test -f components/layout/Navbar.tsx && test -f components/layout/NavbarMobile.tsx && grep -q "sticky top-0 z-50" components/layout/Navbar.tsx && grep -q "'use client'" components/layout/NavbarMobile.tsx && grep -q "aria-label" components/layout/NavbarMobile.tsx && grep -q "setOpen(false)" components/layout/NavbarMobile.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/layout/Navbar.tsx exists with named export `Navbar`
    - components/layout/Navbar.tsx contains `sticky top-0 z-50`
    - components/layout/Navbar.tsx contains `border-b bg-background`
    - components/layout/Navbar.tsx contains `h-16 max-w-5xl`
    - components/layout/Navbar.tsx contains `font-display text-base font-bold` (brand)
    - components/layout/Navbar.tsx contains `#about` and `#projects` and `#resume` and `#contact`
    - components/layout/Navbar.tsx contains `hidden` and `md:flex` (desktop nav hidden on mobile)
    - components/layout/NavbarMobile.tsx exists with `'use client'` directive
    - components/layout/NavbarMobile.tsx contains `useState`
    - components/layout/NavbarMobile.tsx contains `Menu` and `X` imports from lucide-react
    - components/layout/NavbarMobile.tsx contains `aria-label="Toggle menu"`
    - components/layout/NavbarMobile.tsx contains `aria-expanded`
    - components/layout/NavbarMobile.tsx contains `setOpen(false)` (close on link click)
    - components/layout/NavbarMobile.tsx contains `md:hidden` (only visible on mobile)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Sticky navbar renders with "DJ Catan" brand left, 4 anchor links right on desktop, hamburger menu on mobile that opens/closes and navigates to sections.</done>
</task>

<task type="auto">
  <name>Task 3: Build Footer and wire Navbar + Footer into page.tsx</name>
  <files>components/sections/Footer.tsx, app/page.tsx</files>
  <read_first>
    - .planning/phases/02-home-page/02-UI-SPEC.md (Footer component inventory — exact classes, icons, copy)
    - app/page.tsx (current structure — add Navbar import before main, Footer after main)
    - components/layout/Navbar.tsx (just created — verify import path)
  </read_first>
  <action>
**components/sections/Footer.tsx** — Server Component. Named export `Footer`. Create this file (replacing any existing stub or creating fresh).

Structure:
```tsx
import { Github, Linkedin, Mail } from 'lucide-react'

export function Footer() {
  return (
    <footer className="border-t bg-background py-8">
      <div className="mx-auto flex max-w-5xl items-center justify-between px-6">
        <p className="text-sm text-muted-foreground">
          &copy; 2026 DJ Catan
        </p>
        <div className="flex gap-4">
          <a
            href="https://github.com/"
            target="_blank"
            rel="noopener noreferrer"
            aria-label="GitHub"
            className="p-2 text-muted-foreground transition-colors hover:text-primary"
          >
            <Github className="h-5 w-5" />
          </a>
          <a
            href="https://linkedin.com/"
            target="_blank"
            rel="noopener noreferrer"
            aria-label="LinkedIn"
            className="p-2 text-muted-foreground transition-colors hover:text-primary"
          >
            <Linkedin className="h-5 w-5" />
          </a>
          <a
            href="mailto:hello@djcatan.com"
            aria-label="Email"
            className="p-2 text-muted-foreground transition-colors hover:text-primary"
          >
            <Mail className="h-5 w-5" />
          </a>
        </div>
      </div>
    </footer>
  )
}
```

Key requirements per UI-SPEC and CONTEXT D-23/D-24:
- Footer element is `<footer>` (not `<section>`)
- `border-t bg-background py-8` — top border, cream bg, vertical padding
- Copyright left: `text-sm text-muted-foreground`
- Icon links right: `p-2` for 44px touch target, `text-muted-foreground hover:text-primary transition-colors`
- Icons: `Github`, `Linkedin`, `Mail` from lucide-react, all `h-5 w-5` (20px)
- GitHub and LinkedIn have `target="_blank" rel="noopener noreferrer"`
- Email uses `mailto:hello@djcatan.com`
- Each link has `aria-label`
- Social URLs are placeholders (e.g. `https://github.com/`) — DJ fills in real profile URLs before launch

**app/page.tsx** — Update to import and render Navbar before `<main>` and Footer after `<main>`:

```tsx
import { Navbar } from '@/components/layout/Navbar'
import { Hero } from '@/components/sections/Hero'
import { About } from '@/components/sections/About'
import { Projects } from '@/components/sections/Projects'
import { Resume } from '@/components/sections/Resume'
import { Contact } from '@/components/sections/Contact'
import { Footer } from '@/components/sections/Footer'

export default function Home() {
  return (
    <>
      <Navbar />
      <main>
        <Hero />
        <About />
        <Projects />
        <Resume />
        <Contact />
      </main>
      <Footer />
    </>
  )
}
```

Navbar is OUTSIDE `<main>` (it is navigation chrome, not main content). Footer is also OUTSIDE `<main>`. The `<main>` tag wraps only the content sections.
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && test -f components/sections/Footer.tsx && grep -q "Github" components/sections/Footer.tsx && grep -q "Linkedin" components/sections/Footer.tsx && grep -q "Mail" components/sections/Footer.tsx && grep -q "2026 DJ Catan" components/sections/Footer.tsx && grep -q "Navbar" app/page.tsx && grep -q "Footer" app/page.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/Footer.tsx exists with named export `Footer`
    - Footer.tsx contains `border-t bg-background py-8`
    - Footer.tsx contains `text-sm text-muted-foreground` (copyright)
    - Footer.tsx contains `hover:text-primary transition-colors` (icon hover)
    - Footer.tsx contains `aria-label="GitHub"` and `aria-label="LinkedIn"` and `aria-label="Email"`
    - Footer.tsx contains `Github`, `Linkedin`, `Mail` imports from lucide-react
    - Footer.tsx contains `p-2` on each icon link (touch target)
    - Footer.tsx contains `2026 DJ Catan`
    - app/page.tsx imports `Navbar` from `@/components/layout/Navbar`
    - app/page.tsx imports `Footer` from `@/components/sections/Footer`
    - app/page.tsx renders `<Navbar />` before `<main>`
    - app/page.tsx renders `<Footer />` after `</main>`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Footer renders with copyright and 3 social icon links with hover-to-terracotta effect. Page.tsx wires Navbar above main content and Footer below. Full page navigation chrome is complete.</done>
</task>

</tasks>

<verification>
1. `npx next build` exits 0 — no TypeScript or build errors
2. Browser at localhost:3000: page background is warm cream (#FAF8F5), not pure white
3. Sticky navbar visible at top with "DJ Catan" brand and 4 links (About, Work, Resume, Contact)
4. Clicking any navbar link smooth-scrolls to the corresponding section
5. At mobile viewport (< 768px): hamburger icon visible, opens dropdown menu, tapping link closes menu
6. Footer visible at bottom with copyright text and 3 social icons that turn terracotta on hover
</verification>

<success_criteria>
- Warm palette active: bg-background renders #FAF8F5, text-primary renders #C2603A
- Navbar is sticky, z-50, with solid cream background and bottom border
- Desktop: 4 nav links visible. Mobile: hamburger opens/closes menu
- Footer has copyright + 3 icon links (GitHub, LinkedIn, Email) with hover-to-terracotta
- Smooth scroll works on all anchor links
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-01-SUMMARY.md`
</output>
