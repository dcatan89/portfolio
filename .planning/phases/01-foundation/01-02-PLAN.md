---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/projects.ts
  - components/sections/Hero.tsx
  - components/sections/About.tsx
  - components/sections/Projects.tsx
  - components/sections/Resume.tsx
  - components/sections/Contact.tsx
  - app/page.tsx
autonomous: false
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
user_setup:
  - service: vercel
    why: "Deploy pipeline — production URL must exist before phase is complete"
    env_vars: []
    dashboard_config:
      - task: "Connect GitHub repository to Vercel for auto-deploy on push to main"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository"
must_haves:
  truths:
    - "lib/projects.ts exports a typed Project interface and an array of 3 placeholder projects"
    - "Home page renders all 5 section stubs without errors"
    - "Each section stub has an id attribute for anchor link targeting"
    - "A visitor can load a live Vercel URL and see the working app"
    - "2 projects are marked featured: true, 1 is featured: false"
  artifacts:
    - path: "lib/projects.ts"
      provides: "Single source of truth for all project data"
      exports: ["Project", "projects", "featuredProjects", "allProjects"]
      min_lines: 40
    - path: "components/sections/Hero.tsx"
      provides: "Hero section stub with DJ's name and tagline"
      contains: "id=\"hero\""
    - path: "components/sections/About.tsx"
      provides: "About section stub"
      contains: "id=\"about\""
    - path: "components/sections/Projects.tsx"
      provides: "Projects section stub"
      contains: "id=\"projects\""
    - path: "components/sections/Resume.tsx"
      provides: "Resume section stub"
      contains: "id=\"resume\""
    - path: "components/sections/Contact.tsx"
      provides: "Contact section stub"
      contains: "id=\"contact\""
    - path: "app/page.tsx"
      provides: "Home page assembling all section stubs"
      contains: "Hero"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/Hero.tsx"
      via: "import and render"
      pattern: "import.*Hero.*from"
    - from: "app/page.tsx"
      to: "components/sections/Projects.tsx"
      via: "import and render"
      pattern: "import.*Projects.*from"
    - from: "lib/projects.ts"
      to: "components/sections/Projects.tsx"
      via: "data consumed by component (Phase 2 wiring)"
      pattern: "export.*Project"
---

<objective>
Create the project data layer (lib/projects.ts) and all section stubs, wire the home page, and deploy to Vercel so a production URL exists from day one.

Purpose: Establish the data contract that Phase 2 and Phase 4 depend on, and get the site live immediately so every subsequent phase ships to a real URL. The section stubs with id attributes ensure anchor links work when Phase 2 adds navigation.

Output: lib/projects.ts with 3 typed placeholder projects, 5 section stub components, a working home page, and a live Vercel deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/projects.ts data layer with typed placeholder projects</name>
  <files>lib/projects.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-05 -- locked decisions on data shape and placeholder content)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: lib/projects.ts Data Layer -- exact interface and data shape)
  </read_first>
  <action>
    Create `lib/projects.ts` implementing the full Project interface per D-01. This is the SINGLE SOURCE OF TRUTH for all project data consumed by Phase 2 (project cards) and Phase 4 (case study pages).

    1. Define and export the `Project` interface with ALL fields from day one (per D-01 -- do NOT defer fields):
       - `id: string`
       - `slug: string`
       - `title: string`
       - `description: string`
       - `techStack: string[]`
       - `links: { live?: string; github?: string }`
       - `featured: boolean`
       - `problem: string` (case study field -- string per D-03, no MDX)
       - `role: string`
       - `keyDecisions: string`
       - `outcome: string`

    2. Export `const projects: Project[]` with exactly 3 entries per D-02:
       - Project 1: `featured: true`, slug `'TODO-real-slug-1'`, title `'TODO — Real Project Name'` (DJ fills in real names before Phase 2 per CONTEXT.md specifics)
       - Project 2: `featured: true`, slug `'TODO-real-slug-2'`, title `'TODO — Real Project Name'`
       - Project 3: `featured: false`, slug `'project-3'`, title `'TODO — Non-Featured Project'`, empty string case study fields

    3. Use DJ-flavored realistic placeholder text per D-04:
       - Descriptions should sound like real project descriptions, not lorem ipsum
       - Case study content fields should have placeholder prompts like "Placeholder: what problem did this project solve?"
       - techStack arrays should use real technology names (e.g., `['Next.js', 'TypeScript', 'Tailwind CSS']`)
       - links should have `live: undefined, github: undefined` (no fake URLs)

    4. Export convenience filters:
       - `export const featuredProjects = projects.filter((p) => p.featured)`
       - `export const allProjects = projects`

    5. Run `npx tsc --noEmit` to confirm the file is type-safe.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/projects.ts` exports `Project` interface with all 11 fields: id, slug, title, description, techStack, links, featured, problem, role, keyDecisions, outcome
    - `lib/projects.ts` exports `projects` array with exactly 3 entries
    - `lib/projects.ts` exports `featuredProjects` and `allProjects`
    - First two projects have `featured: true`, third has `featured: false`
    - Slugs for featured projects contain `TODO` (per CONTEXT.md -- DJ fills in real slugs)
    - Third project has empty strings for problem, role, keyDecisions, outcome
    - `links` field on all projects uses `undefined` for live and github (no fake URLs)
    - `techStack` arrays contain real technology names (not placeholder text)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>lib/projects.ts exists with full typed Project interface, 3 placeholder projects (2 featured, 1 not), and convenience exports. Type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create section stubs and wire home page</name>
  <files>components/sections/Hero.tsx, components/sections/About.tsx, components/sections/Projects.tsx, components/sections/Resume.tsx, components/sections/Contact.tsx, app/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Section Stubs -- must return semantic section elements with id attributes, NOT null)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 -- DJ-flavored realistic placeholder text; Claude's discretion on stub structure)
    - app/layout.tsx (current state -- confirm font classes available: font-display, font-body)
    - app/globals.css (confirm @theme tokens: --font-display, --font-body)
  </read_first>
  <action>
    Create 5 section stub components and wire them into the home page. Every stub MUST return a `<section>` element with an `id` attribute -- NEVER return null. This is critical: returning null breaks anchor navigation in Phase 2.

    1. Create `components/sections/Hero.tsx` (Server Component -- no 'use client'):
       ```tsx
       export function Hero() {
         return (
           <section id="hero" className="flex min-h-screen items-center justify-center">
             <div className="text-center">
               <h1 className="font-display text-5xl font-bold">DJ Catan</h1>
               <p className="mt-4 text-lg text-gray-600">
                 Full-stack developer. Building thoughtful digital products.
               </p>
             </div>
           </section>
         )
       }
       ```

    2. Create `components/sections/About.tsx`:
       ```tsx
       export function About() {
         return (
           <section id="about" className="py-24 px-8">
             <h2 className="font-display text-3xl font-bold">About</h2>
             <p className="mt-4 max-w-2xl text-gray-600">
               Hey, I'm DJ. I build things for the web and care about getting the details right.
               More about me coming soon.
             </p>
           </section>
         )
       }
       ```

    3. Create `components/sections/Projects.tsx`:
       ```tsx
       export function Projects() {
         return (
           <section id="projects" className="py-24 px-8">
             <h2 className="font-display text-3xl font-bold">Projects</h2>
             <p className="mt-4 text-gray-600">Featured work coming soon.</p>
           </section>
         )
       }
       ```

    4. Create `components/sections/Resume.tsx`:
       ```tsx
       export function Resume() {
         return (
           <section id="resume" className="py-24 px-8">
             <h2 className="font-display text-3xl font-bold">Resume</h2>
             <p className="mt-4 text-gray-600">Download link coming soon.</p>
           </section>
         )
       }
       ```

    5. Create `components/sections/Contact.tsx`:
       ```tsx
       export function Contact() {
         return (
           <section id="contact" className="py-24 px-8">
             <h2 className="font-display text-3xl font-bold">Get in Touch</h2>
             <p className="mt-4 text-gray-600">
               Contact form coming soon. In the meantime, feel free to reach out directly.
             </p>
           </section>
         )
       }
       ```

    6. Update `app/page.tsx` to import and render all 5 section stubs in order:
       ```tsx
       import { Hero } from '@/components/sections/Hero'
       import { About } from '@/components/sections/About'
       import { Projects } from '@/components/sections/Projects'
       import { Resume } from '@/components/sections/Resume'
       import { Contact } from '@/components/sections/Contact'

       export default function Home() {
         return (
           <main>
             <Hero />
             <About />
             <Projects />
             <Resume />
             <Contact />
           </main>
         )
       }
       ```

    All section stubs are Server Components (no 'use client' directive). Per STATE.md, only ContactForm.tsx (Phase 3) will be a client component.

    7. Run `npm run build` to confirm all imports resolve and page renders.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/Hero.tsx` exists and contains `id="hero"`
    - `components/sections/About.tsx` exists and contains `id="about"`
    - `components/sections/Projects.tsx` exists and contains `id="projects"`
    - `components/sections/Resume.tsx` exists and contains `id="resume"`
    - `components/sections/Contact.tsx` exists and contains `id="contact"`
    - None of the section stubs contain `'use client'` directive
    - None of the section stubs return `null`
    - `app/page.tsx` imports all 5 section components using `@/components/sections/` path alias
    - `app/page.tsx` renders Hero, About, Projects, Resume, Contact in that order inside a `<main>` element
    - Each section stub uses `font-display` class on headings (proving Tailwind @theme font token works)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 5 section stubs exist with semantic section elements and id attributes. Home page assembles them in order. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to Vercel and verify live URL</name>
  <files>none -- deployment only</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (specifics: deployed Vercel URL should work immediately)
  </read_first>
  <action>
    Push all code to GitHub main branch using `git push origin main`. The user will then connect the repo to Vercel for auto-deploy. After deployment, the user verifies the live site loads correctly.
  </action>
  <what-built>
    Complete Next.js 15 app with:
    - Tailwind v4 CSS-first configuration
    - Playfair Display + Inter fonts loaded via next/font
    - lib/projects.ts with 3 typed placeholder projects
    - 5 section stubs (Hero, About, Projects, Resume, Contact) with anchor-ready id attributes
    - Home page assembling all sections
  </what-built>
  <how-to-verify>
    1. Push code to GitHub (main branch) if not already pushed
    2. Connect the GitHub repo to Vercel:
       - Go to https://vercel.com/new
       - Import the portfolio repository
       - Accept default settings (Next.js auto-detected)
       - Click Deploy
    3. Wait for build to complete (should take ~60 seconds)
    4. Open the production URL (e.g., portfolio-xxx.vercel.app)
    5. Verify:
       - Page loads without errors (no white screen, no 500)
       - "DJ Catan" heading is visible in Playfair Display font (serif, editorial look)
       - Body text is in Inter font (clean sans-serif)
       - Scrolling down shows all 5 sections: Hero, About, Projects, Resume, Contact
       - No console errors in browser DevTools
    6. Copy the production URL -- it will be needed for metadataBase in future phases
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Code is pushed to GitHub main branch
    - Vercel deployment completes without build errors
    - Production URL returns a 200 response
    - "DJ Catan" heading visible on the live page
    - All 5 sections visible when scrolling (Hero, About, Projects, Resume, Contact)
  </acceptance_criteria>
  <done>Live Vercel URL exists and loads the working Next.js app with all section stubs visible.</done>
  <resume-signal>Paste the Vercel production URL and type "approved", or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes with exit code 0
2. `npm run lint` passes
3. `lib/projects.ts` exports Project interface with all 11 fields
4. `lib/projects.ts` has exactly 3 projects (2 featured, 1 non-featured)
5. All 5 section stubs have `id` attributes matching: hero, about, projects, resume, contact
6. No section stub returns null
7. `app/page.tsx` imports and renders all 5 sections
8. Live Vercel URL returns 200 and shows the working app
</verification>

<success_criteria>
- lib/projects.ts is the single source of truth with full typed data shape (all fields present, not deferred)
- Home page renders all sections without errors
- Every section has an id attribute ready for Phase 2 anchor links
- A visitor can load a real Vercel URL and see the working site
- Placeholder content is DJ-flavored realistic text, not lorem ipsum or test fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
