---
phase: 04-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/projects/[slug]/page.tsx
  - components/sections/Projects.tsx
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "Visitor can navigate to /projects/TODO-real-slug-1 and see a full case study page with Problem, My Role, Key Decisions, and Outcome sections"
    - "Visitor clicking a featured project card on the home page lands on the correct case study page"
    - "Non-featured project slugs return 404 (dynamicParams = false)"
    - "Case study pages are statically generated at build time"
  artifacts:
    - path: "app/projects/[slug]/page.tsx"
      provides: "Case study page with static generation"
      exports: ["default", "generateStaticParams", "generateMetadata"]
      min_lines: 80
    - path: "components/sections/Projects.tsx"
      provides: "Featured cards wrapped in Link to case study pages"
      contains: "Link"
  key_links:
    - from: "components/sections/Projects.tsx"
      to: "app/projects/[slug]/page.tsx"
      via: "Next.js Link with /projects/${slug} href"
      pattern: "href.*projects/"
    - from: "app/projects/[slug]/page.tsx"
      to: "lib/projects.ts"
      via: "import featuredProjects for generateStaticParams + data lookup"
      pattern: "featuredProjects"
---

<objective>
Build the case study page and wire project card navigation so visitors can read full project narratives.

Purpose: PROJ-02 requires each featured project to have a dedicated page with Problem, My Role, Key Decisions, and Outcome sections. This plan creates the page and makes cards link to it.
Output: `app/projects/[slug]/page.tsx` (new), updated `components/sections/Projects.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-case-studies/04-CONTEXT.md

@lib/projects.ts
@components/sections/Projects.tsx
@components/sections/About.tsx
@components/sections/Resume.tsx
@app/layout.tsx
@app/page.tsx
@app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/projects.ts:
```typescript
export interface Project {
  id: string
  slug: string
  title: string
  description: string
  techStack: string[]
  links: { live?: string; github?: string }
  featured: boolean
  problem: string
  role: string
  keyDecisions: string
  outcome: string
}

export const featuredProjects = projects.filter((p) => p.featured)
```

From app/layout.tsx:
```typescript
// Title template is already set: '%s | DJ Catan'
// Navbar is NOT in layout — it's per-page in app/page.tsx
// Font variables: --font-playfair-display (display), --font-inter (body)
// CSS classes: font-display (Playfair), font-body (Inter)
```

From components/sections/Resume.tsx (terracotta CTA button style):
```typescript
// className="rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground transition-colors hover:bg-primary/90"
```

From components/sections/About.tsx (photo placeholder pattern):
```typescript
// className="flex aspect-[3/4] w-full max-w-xs items-center justify-center rounded-2xl bg-[#E8E4DF]"
// Uses lucide-react User icon as placeholder
```

From components/sections/Projects.tsx (tech stack pill style):
```typescript
// className="rounded-full bg-muted px-3 py-1 text-sm text-muted-foreground"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case study page with static generation</name>
  <files>app/projects/[slug]/page.tsx</files>
  <action>
Create `app/projects/[slug]/page.tsx` as a Server Component (no 'use client'). Per D-18.

**Static generation (per D-16, D-17):**
- `generateStaticParams`: return slugs from `featuredProjects` only (import from `@/lib/projects`)
- `export const dynamicParams = false` — non-featured slugs return 404
- `generateMetadata`: return `{ title: project.title, description: project.description }` — the layout template '%s | DJ Catan' handles the suffix (per D-19)

**Page layout (per D-01, D-02, D-03):**
- Single-column prose layout, centered with `max-w-3xl mx-auto`
- Generous vertical spacing between sections using `pt-12` gaps
- Wrap everything in `<main className="px-6 py-12 md:py-24">`

**Page structure (top to bottom, per D-06 through D-11):**

1. **Navbar** — import `Navbar` from `@/components/layout/Navbar` and render at the top (same pattern as `app/page.tsx` — Navbar is per-page, NOT in layout)

2. **Back link (per D-06):** `<Link href="/#projects">` with text "All projects" and a left arrow character. Style: `text-sm text-muted-foreground hover:text-primary transition-colors`. Place inside the `max-w-3xl` content column.

3. **Page heading (per D-07):** Project `title` in `font-display text-4xl md:text-5xl font-bold text-foreground`. Project `description` below in `mt-4 text-lg text-muted-foreground`.

4. **Hero image placeholder (per D-04, D-05):** `aspect-video` container with `bg-muted rounded-xl` and a centered `ImageIcon` from lucide-react (or text label "Project screenshot"). Full width of content column. Adapt the About.tsx placeholder pattern but use 16:9 aspect ratio instead of 3:4.

5. **Four content sections (per D-09, D-02):** Problem, My Role, Key Decisions, Outcome. Each section:
   - Heading: `font-display text-2xl md:text-3xl font-bold text-foreground`
   - Body: `mt-4 text-foreground leading-relaxed`
   - Spacing: `pt-12` between sections
   - Data sourced from `project.problem`, `project.role`, `project.keyDecisions`, `project.outcome`

6. **Tech stack tags (per D-10):** Below Outcome section with `pt-12`. Reuse exact pill style from Projects.tsx: `rounded-full bg-muted px-3 py-1 text-sm text-muted-foreground`. Wrap in a flex container with `gap-2`.

7. **External links (per D-11):** Below tech tags with `pt-8`. Two CTA buttons:
   - Live site: `rounded-lg bg-primary px-8 py-3 font-bold text-primary-foreground transition-colors hover:bg-primary/90` (terracotta, same as Resume download button)
   - GitHub: `rounded-lg border border-border px-8 py-3 font-bold text-foreground transition-colors hover:bg-muted` (outlined variant)
   - Only render each button if the corresponding link exists (`project.links.live`, `project.links.github`)
   - Both open in new tab (`target="_blank" rel="noopener noreferrer"`)

**Data lookup:** In the page component function, find the project by matching `params.slug` against `featuredProjects`. Use `notFound()` from `next/navigation` as a fallback (though `dynamicParams = false` should prevent this).

**Import pattern:** Use `import Link from 'next/link'`, `import { notFound } from 'next/navigation'`, `import { Navbar } from '@/components/layout/Navbar'`, `import { featuredProjects } from '@/lib/projects'`, `import { Image as ImageIcon } from 'lucide-react'` (for hero placeholder icon).

Note: Next.js 15 App Router passes params as a Promise. The page component signature should be: `export default async function CaseStudyPage({ params }: { params: Promise<{ slug: string }> })` and use `const { slug } = await params` inside the function body. Same applies to `generateMetadata`.
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Case study page exists at app/projects/[slug]/page.tsx, builds without errors, generates static pages for featured project slugs only, renders all four content sections with proper styling</done>
</task>

<task type="auto">
  <name>Task 2: Wire featured project cards as links to case study pages</name>
  <files>components/sections/Projects.tsx</files>
  <action>
Update `components/sections/Projects.tsx` to make featured project cards clickable links to their case study pages.

**Changes to ProjectCard component (per D-12, D-13, D-14, D-15):**

1. Import `Link` from `next/link` at the top of the file.

2. Modify the `ProjectCard` function: if `project.featured` is true, wrap the entire `<article>` element in `<Link href={`/projects/${project.slug}`}>`. If not featured, render `<article>` as-is (no link wrapper).

3. Add `cursor-pointer` to the article's className when the project is featured (per D-14). The existing `hover:-translate-y-1 hover:shadow-md` transitions stay unchanged.

4. Do NOT add a separate "Read case study" text link (per D-15) — the whole card IS the entry point.

**Implementation approach:** Use a conditional wrapper pattern. Either:
- Render `<Link href={...}><article ...>...</article></Link>` for featured
- Render `<article ...>...</article>` for non-featured

Make sure the existing icon links (ExternalLink, GitBranch) inside project cards still work for non-featured cards. For featured cards, these icon links are less important since the card links to the case study page (which has its own CTA buttons at the bottom), but they should still render correctly. If the icons cause nested-link issues (a inside Link), conditionally hide them for featured cards or use `e.stopPropagation()` — simplest approach is to just not render the icon links section for featured cards since the case study page has full CTA buttons.
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Featured project cards are wrapped in Link to /projects/[slug], non-featured cards remain unchanged, build succeeds with no errors, no nested anchor warnings</done>
</task>

</tasks>

<verification>
1. `npx next build` exits 0 — no TypeScript errors, all static pages generated
2. Build output shows `/projects/TODO-real-slug-1` and `/projects/TODO-real-slug-2` as statically generated routes
3. `grep -n "Link" components/sections/Projects.tsx` confirms Link import and usage
4. `grep -n "generateStaticParams" app/projects/\[slug\]/page.tsx` confirms static generation
5. `grep -n "dynamicParams" app/projects/\[slug\]/page.tsx` confirms false setting
</verification>

<success_criteria>
- Build succeeds with case study pages statically generated for both featured project slugs
- Case study page renders: back link, title, description, hero placeholder, Problem, My Role, Key Decisions, Outcome, tech tags, and external link CTAs
- Featured project cards are clickable links; non-featured cards are not
- No 'use client' directive on the case study page (Server Component)
</success_criteria>

<output>
After completion, create `.planning/phases/04-case-studies/04-01-SUMMARY.md`
</output>
