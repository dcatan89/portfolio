---
phase: 05-polish-launch
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - POLISH-03

must_haves:
  truths:
    - "Home page is visually correct on 375px mobile viewport"
    - "Home page is visually correct on 768px tablet viewport"
    - "Home page is visually correct on 1280px desktop viewport"
    - "Case study page is visually correct on 375px mobile viewport"
  artifacts: []
  key_links: []
---

<objective>
Responsive visual verification checkpoint: confirm the fully animated, SEO-complete site looks correct across mobile (375px), tablet (768px), and desktop (1280px) viewports.

Purpose: Catch layout issues before launch (POLISH-03). Per D-19, this is a manual checkpoint — code changes only if obvious issues surface (D-20, minor Tailwind tweaks only).
Output: Human sign-off on responsive quality, plus any minor inline fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-launch/05-CONTEXT.md
@.planning/phases/05-polish-launch/05-01-SUMMARY.md
@.planning/phases/05-polish-launch/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy to Vercel for responsive testing</name>
  <files></files>
  <read_first>
    package.json
  </read_first>
  <action>
    1. Run `npm run build` to verify everything compiles cleanly after Plans 01 and 02.
    2. Deploy to Vercel: `npx vercel --prod --yes` (project is already linked from prior phases).
    3. Capture and print the production URL for the checkpoint task.
    4. Also start a local dev server with `npm run dev` in the background so the user can test locally if preferred.
  </action>
  <verify>
    <automated>cd /Users/djcatan/portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Vercel deployment URL is captured and printed
  </acceptance_criteria>
  <done>Build passes and site is deployed to Vercel production URL, ready for responsive verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive visual verification across breakpoints</name>
  <files></files>
  <action>
    Present the responsive verification checklist to the user. The user will test the deployed site at three viewport widths (375px, 768px, 1280px) and verify animations, accessibility, and SEO endpoints. If minor layout issues are found (per D-20), apply Tailwind class fixes inline.
  </action>
  <verify>Human approves responsive quality or describes issues for inline fixing.</verify>
  <done>Human confirms all 25 checklist items pass across mobile, tablet, and desktop viewports.</done>
  <what-built>
    Complete Polish and Launch phase: Motion scroll-reveal animations on all sections, Hero stagger animation, project card cascade, skip-to-content link, focus rings on all interactive elements, complete SEO metadata with OG image, sitemap, and robots.txt.
  </what-built>
  <how-to-verify>
    Open the Vercel production URL (or http://localhost:3000) in your browser. Test each viewport using DevTools responsive mode or a real device:

    **375px (iPhone SE) — Home Page:**
    1. Navbar hamburger menu opens and closes correctly
    2. Hero text is readable and not overflowing
    3. About section layout is single-column
    4. Project cards stack vertically, not overlapping
    5. Resume section centered and readable
    6. Contact form and social links are usable
    7. Footer links wrap cleanly

    **375px (iPhone SE) — Case Study Page:**
    8. Navigate to any featured project case study
    9. Back link, heading, sections all fit within viewport
    10. CTA buttons are full-width or properly sized

    **768px (iPad) — Home Page:**
    11. Navbar shows desktop links (no hamburger)
    12. Project cards show in grid (not single column)
    13. Contact form two-column layout works or gracefully stacks

    **1280px (Desktop) — Home Page:**
    14. Full desktop layout, no horizontal scroll
    15. Project grid shows expected columns
    16. All sections have appropriate spacing

    **Animations (any viewport):**
    17. Scroll down slowly — sections fade up as they enter viewport
    18. Hero elements appear with stagger on page load
    19. Project cards cascade in

    **Accessibility (any viewport):**
    20. Press Tab — skip-to-content link appears at top-left
    21. Continue tabbing — all links/buttons have visible focus rings
    22. Skip link jumps to main content area

    **SEO (desktop):**
    23. View page source — no `noindex` meta tag present
    24. Visit /sitemap.xml — lists home and project pages
    25. Visit /robots.txt — allows all crawlers
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues you see (minor Tailwind fixes can be applied inline per D-20).</resume-signal>
</task>

</tasks>

<verification>
- Human confirms responsive quality at 375px, 768px, 1280px
- Human confirms animations work smoothly
- Human confirms accessibility basics (skip link, focus rings)
- Human confirms SEO endpoints (/sitemap.xml, /robots.txt, no noindex)
</verification>

<success_criteria>
1. Human approves responsive visual quality across all three breakpoints
2. No critical layout issues on mobile, tablet, or desktop
3. Animations are smooth and subtle (not janky)
4. Skip-to-content and focus rings confirmed working
5. OG metadata and sitemap verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-launch/05-03-SUMMARY.md`
</output>
